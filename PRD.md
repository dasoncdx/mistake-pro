# 错题Pro — 产品需求文档（PRD）

> 版本：v1.1 | 状态：Phase 1 已部署 | 线上：https://mistake-pro.zeabur.app

---

## 1. 产品一句话

**让每一道错题，变成学生知识版图上被征服的领地。**

---

## 2. 解决的问题

### 2.1 三个核心痛点

| # | 痛点 | 现象 | 根因 |
|---|------|------|------|
| 1 | **厌烦感** | 孩子不愿抄错题，抄了也觉得没用 | 机械劳动只有成本，没有即时收益 |
| 2 | **假看** | 复习错题时看到原题和答案，条件反射"哦选A"，不思考 | 大脑用再认（recognition）替代重现（reproduction） |
| 3 | **同类题匮乏** | 家长知道该练同类题，但拿不出来 | 举一反三依赖"会出题的人"，这个能力稀缺 |

### 2.2 四个更深层维度

| 维度 | 要解决的问题 |
|------|------------|
| **认知** | 如何精准诊断错因、生成有效变式？ |
| **时间** | 错题贯穿一学期，如何科学安排复习频率？如何防止滚雪球？ |
| **动机** | 做曾经错过的题天然是"hard模式"，孩子如何愿意坚持？ |
| **社会** | 家长有了解的诉求，但如何不变成"监工"？ |

---

## 3. 目标用户

### 3.1 学生

- 范围：小学一年级 ~ 高中三年级
- 行为：录入错题 → 做变式练习题 → 看知识地图
- 核心诉求：不想抄题、不想"假学习"、最好有趣
- 核心障碍：面对薄弱点天然想回避；做错过的题比别人更hard

### 3.2 家长

- 行为：查看知识版图报告 → 获取行动建议 → 配合辅导
- 核心诉求：知道孩子哪里薄弱、知道怎么帮、看到进步趋势
- 核心障碍：信息不足只能当"监工"；想帮但不知道该具体做什么

### 3.3 两者关系

```
学生 ──（录入错题、做变式题、看知识地图）──→ 错题助手
                                                 │
家长 ──（查看诊断报告、获取行动指南）────────────→ │
       （浏览错题原文、练习记录）                  │
       （下载/导出错题与练习数据）                 │
```

**家长数据接触权限**：
- 默认视图：知识版图 + 诊断报告 + 行动建议（教练视角）
- 可按需浏览：错题原文 + 学生作答记录（了解具体状况）
- 导出功能：支持导出错题集、变式题集为PDF/打印格式

---

## 4. 核心设计原则

1. **变式为主，原题为辅** — 日常复习用全新变式题，杜绝"假看"。期中/期末总复习时，可穿插原题重现——尤其真题本身值得反复练熟。关键是"不每次都看同一道"，而非"永远不能看"
2. **知识点驱动出题量** — 每次复习会话3-5道题。后台先对已上传错题做结构化归并，提取出涉及的知识点集合，再按调度周期决定本轮覆盖哪些知识点、每个知识点出几道，而非拍脑袋固定数量。核心逻辑：**先看有哪些知识点该练，再分配题目数量**
3. **先让孩子赢一次** — 变式题难度阶梯：简化版→标准版→挑战版
4. **夸过程不夸结果** — 反馈具体指出"哪个行为带来了进步"
5. **家长是教练不是监工** — 家长看到的是行动建议 + 按需浏览原文，不是监控面板

### 4.1 叙事转换

- 传统叙事： "这道题我错了，我要改正它" → 挫败感
- 新叙事： "发现一个漏洞，补上它我的知识版图就更完整" → 掌控感
- 知识地图：每个知识点是一块领地，做错=出现漏洞，变式做对=修复，连续征服=点亮

---

## 5. 功能清单与优先级

### 5.1 总览

| 优先级 | 功能 | 阶段 | 说明 |
|--------|------|------|------|
| **P0** | F-1 账号认证 | Phase 1 | 注册/登录/密码管理，测试账号预置 |
| **P0** | F0 初始设置 | Phase 1 | 首次启动：昵称、城市、年级、教材版本、科目多选 |
| **P0** | F1 错题录入 → AI诊断 | Phase 1 | 文字输入 + 拍照OCR，AI定位知识点、判定错误类型 |
| **P0** | F2 AI生成变式题 | Phase 1 | 同考点变式，每次全新，不附原题 |
| **P0** | F3 作答批改反馈 | Phase 1 | AI批改，鼓励性反馈 |
| **P0** | F4 数据持久化 | Phase 1 | profile.json + 目录结构 + SQLite |
| **P0** | F5 审计模块 | Phase 1 | 代码自检 + 测试数据 + 审计报告 |
| **P1** | F5 分层退出机制 | Phase 2 | 活跃→观察→休眠三池流转 |
| **P1** | F6 间隔重复调度 | Phase 2 | 对齐学期节奏的SM-2算法 |
| **P1** | F7 每日复习提醒 | Phase 2 | 到期知识点自动推送 |
| **P2** | F8 回合战斗游戏层 | Phase 3 | 选角、战斗、知识地图 |
| **P2** | F9 IP图片支持 | Phase 3 | 千人千面，自选角色 |
| **P2** | F10 知识地图 | Phase 3 | 征服进度可视化 |
| **P3** | F11 家长报告 | Phase 4 | 诊断报告 + 行动建议 |
| **P3** | F12 课程知识库 | Phase 4 | 结构化课程大纲 |

### 5.2 P0 功能详解（Phase 1 必须交付）

---

#### F0：初始设置（仅首次启动）

**触发时机**：首次启动系统，或 `user_data/` 下无 profile.json 时。

**设置表单**：

```
🎒 欢迎使用错题助手！请先完成初始设置：

  昵称：[________]

  所在地区：
    省：[  选择  ▼]  →  市：[  选择  ▼]  →  区：[  选择  ▼]
    （三级级联，选完省出现市，选完市出现区）

  在读年级：
    ○ 一年级 ○ 二年级 ○ 三年级 ○ 四年级 ○ 五年级 ○ 六年级
    ○ 初一  ○ 初二  ○ 初三
    ○ 高一  ○ 高二  ○ 高三

  教材版本：
    ○ 人教版（默认） ○ 北师大版 ○ 苏教版 ○ 沪教版 ○ 其他：[____]

  需要管理的科目（可多选）：
    ☑ 数学  ☐ 英语  ☐ 语文

  [确认，开始使用]
```

**确认后系统自动执行**：
1. 写入 `user_data/{昵称}/profile.json`
2. 为每个所选科目创建 `user_data/{昵称}/photos/{subject}/` 目录
3. 创建 `user_data/{昵称}/ip_images/{hero,enemy,background}/` 目录
4. 初始化 `user_data/{昵称}/mistakes.db`（含4张表）
5. 进入主菜单

**后续启动**：检测到 profile.json 存在 → 跳过设置，直接进主菜单。如需修改（换年级等），在设置菜单中手动修改。

**profile.json 格式**：

```json
{
  "student_name": "小明",
  "province": "广东省",
  "city": "广州市",
  "district": "天河区",
  "grade_level": "grade_4",
  "curriculum_version": "人教版",
  "subjects": ["math", "english"]
}
```

**设计决策**：不用SQLite存配置，就用一个JSON文件。一行配置，读写都简单，手改也可以。

**验收标准**：
- [ ] 首次启动展示完整设置表单
- [ ] 昵称不为空才能确认
- [ ] 科目至少选1个才能确认（没有科目没法用）
- [ ] 确认后 profile.json、目录结构、数据库全部自动创建
- [ ] 再次启动直接进主菜单，不再展示设置
- [ ] 设置菜单中可修改配置

---

#### F1：错题录入 → AI诊断

**两种录入方式**：

| 方式 | 适用场景 | 流程 |
|------|---------|------|
| **文字输入** | 手头没拍照，或题目是手写/印刷模糊 | CLI中直接打字输入题目 + 错误答案 |
| **拍照OCR** | 试卷/练习册上的错题 | 将照片放入 `photos/{subject}/` → 系统OCR识别 → 用户编辑确认 → 提交诊断 |

**拍照OCR详细流程**：

```
1. 用户将错题照片放入 photos/{subject}/ 目录
2. 系统选择照片 → 调用Claude Vision识别图中题目文字 + 学生答案
3. 系统展示识别结果，进入【编辑确认界面】：
   ┌──────────────────────────────────────┐
   │  📷 OCR识别结果（请检查并修改）        │
   │                                       │
   │  题目：                               │
   │  ┌──────────────────────────────────┐ │
   │  │ 小明买了3/4千克苹果，吃了1/4千克， │ │
   │  │ 还剩多少千克？                    │ │
   │  └──────────────────────────────────┘ │
   │  （可编辑修改）                        │
   │                                       │
   │  你的错误答案：                        │
   │  ┌──────────────────────────────────┐ │
   │  │ 1/2                               │ │
   │  └──────────────────────────────────┘ │
   │  （可编辑修改）                        │
   │                                       │
   │  [确认提交]  [重新拍照]  [改为手动输入] │
   └──────────────────────────────────────┘
4. 用户修改无误后点确认 → 进入AI诊断
5. 确认后的题目文本 + 错误答案存入数据库
6. 原图保留在 photos/{subject}/，文件名加时间戳
```

**设计要点**：
- OCR识别允许错漏，关键是不让错漏进入后续流程——用"用户确认"作为最后一道闸
- 确认界面同时支持修改，错几个字用户直接改，不用重新拍照
- 提供"改为手动输入"退出路径：如果OCR识别太差，用户可直接放弃，改为纯文字输入

**输入**：题目文本/图片 + 学生错误答案（年级和教材版本从 profile.json 自动读取）

**AI输出**：
```
knowledge_point:  "分数的通分"
error_type:       "knowledge_gap" | "thinking_error" | "careless"
error_analysis:   "学生用分子分母分别相加，说明没理解通分的意义..."
correct_answer:   "5/6"
```

**用户故事**：
> 作为学生，我把作业中做错的题目输入系统，系统自动帮我分析错因，我不需要手抄，也不用自己去猜哪里错了。

**验收标准**：
- [ ] 支持两种录入方式：文字输入 + 拍照OCR
- [ ] 拍照后系统调用Claude Vision识别题目文字和错误答案
- [ ] OCR识别结果展示在编辑确认界面，用户可修改
- [ ] 用户确认提交后才进入AI诊断
- [ ] 用户可选择"改为手动输入"放弃OCR结果
- [ ] 确认后的题目文本存入 mistakes 表，原图保留在 photos 目录
- [ ] 学生文字输入错题，效果等同于OCR确认后流程
- [ ] 系统调用AI返回JSON，包含4个字段：knowledge_point / error_type / error_analysis / correct_answer
- [ ] error_type 严格为三选一，不出现自定义类型
- [ ] AI返回非法JSON时自动重试，最多2次
- [ ] 重试仍失败则提示用户"请稍后重试"，不崩溃

---

#### F2：AI生成变式练习题

**输入**：knowledge_point + error_type + error_analysis + grade_level + count（按知识点覆盖情况动态决定，通常每个知识点1-3道，单次会话总计3-5道）

**关键约束**：
- 后台先对所有活跃错题做知识点归并，决定本轮覆盖哪些知识点+每个知识点出几道
- 整体"知识点×题目数"驱动，而非固定数字
- **原题文本不传入**此prompt（日常复习时），仅传入诊断元数据，杜绝AI锚定原题。期中/期末复习时可选择性传入原题进行穿插
- 首题难度 easy（先让孩子赢），后续 same / slightly_harder
- 改变场景、人物、数字、表述，保持同一考点

**AI输出**：
```
[{problem: "...", correct_answer: "...", difficulty: "easy"},
 {problem: "...", correct_answer: "...", difficulty: "same"},
 {problem: "...", correct_answer: "...", difficulty: "slightly_harder"}]
```

**用户故事**：
> 作为学生，系统不是让我重新看原题，而是给我全新的同类题目。我每做对一道，就离征服这个知识点更近一步。

**验收标准**：
- [ ] 生成3道变式题
- [ ] 原题与任一变式题明显不同（非仅改数字，至少改变了场景或问法）
- [ ] AI返回合法JSON数组，包含3个元素
- [ ] 变式题存入`variants`表，关联到原始mistake_id

---

#### F3：学生作答 → AI批改反馈

**输入**：变式题 + 正确答案 + 学生答案 + error_analysis

**AI输出**：
```
is_correct:         true|false
same_error_pattern: true|false|null
feedback:           "你对通分的理解很到位，这次没有犯之前的错误..."
hint:               "试试看先把两个分母找出来？" | null
```

**反馈原则**：
- 做对：具体夸过程，不空洞表扬"你真棒"
- 做错：指出方向，不给完整答案，以问题收尾引导思考
- 宽容等价形式：0.5 = 1/2 = 50%
- **不提及原错题**："你上次..."这类表述在反馈中禁止出现

**用户故事**：
> 作为学生，我做错了有人教我怎么想，做对了有人告诉我哪里做得好。不是冷冰冰的对错判断。

**验收标准**：
- [ ] 系统接受学生答案输入
- [ ] 正确判定正确/错误（含等价形式容忍）
- [ ] 反馈文案 2-4 句
- [ ] 反馈不出现任何暗示"这是你之前错过的题"的表述
- [ ] same_error_pattern 判定准确
- [ ] 作答结果存入`attempts`表

---

#### F4：数据持久化

**存储分工**：

| 数据类型 | 存在哪 | 原因 |
|---------|--------|------|
| 用户配置 | `profile.json` | 一行JSON，读写简单 |
| 错题文本 + 诊断结果 | SQLite `mistakes` 表 | 结构化，需查询 |
| AI生成的变式题 | SQLite `variants` 表 | 需关联查询、去重、统计 |
| 学生作答记录 | SQLite `attempts` 表 | 需统计计算掌握度 |
| 知识点掌握度 | SQLite `knowledge_mastery` 表 | 需按时间查询到期复习 |
| 错题照片（OCR原图） | 文件系统 `photos/{subject}/` | 二进制大文件，留底可查 |
| IP游戏图片 | 文件系统 `ip_images/` | 孩子自己上传的图片 |

**完整目录结构**：

```
user_data/{student_name}/
├── profile.json               # 用户配置
├── mistakes.db                # SQLite（mistakes/variants/attempts/knowledge_mastery）
├── photos/                    # 错题照片（OCR原图留底）
│   ├── math/
│   ├── english/
│   └── chinese/
├── ip_images/                 # 游戏IP图片
│   ├── hero/
│   ├── enemy/
│   └── background/
└── exports/                   # 导出文件（可选，比如打印版变式题）
```

**用户故事（隐含）**：
> 作为系统，所有数据存在本地，重新打开时数据都在，不需要网络同步。

**验收标准**：
- [ ] 4张表正确创建：mistakes / variants / attempts / knowledge_mastery
- [ ] 初始设置后 profile.json + 全部目录自动创建
- [ ] 错题录入后 mistakes 有新记录
- [ ] 变式题生成后 variants 有新记录
- [ ] 学生作答后 attempts 有新记录
- [ ] knowledge_mastery 随作答自动更新
- [ ] 所有数据为本地文件，不依赖任何外部服务

---

### 5.3 P1 功能详解（Phase 2）

---

#### F5：分层退出机制

三池流转规则：

```
     刚做错              连续3次变式正确
        ↓                   + 间隔≥3天
    [活跃池] ──────────────────→ [观察池]
       ↑                            │
       │              连续2次抽查正确 + 间隔≥7天
       │                            ↓
       │                        [休眠池]
       │                            │
       └──────── 休眠池变式做错 ─────┘
               （重新激活）
```

| 池 | 含义 | 日常行为 |
|----|------|---------|
| active | 正在攻克 | 每天/隔天出变式 |
| observing | 在观察中 | 每周随机1题抽查 |
| dormant | 已掌握 | 不出现，期中/期末唤醒 |

**模拟数据**：全学期60道错题 → 日常活跃5-10道

**验收标准**：
- [ ] 新错题自动进入 active
- [ ] 连续3次正确 → active → observing
- [ ] 连续2次抽查正确 → observing → dormant
- [ ] dormant变式做错 → dormant → active
- [ ] pool_status 在 mistakes 和 knowledge_mastery 表同步更新

---

#### F6：间隔重复调度

**算法**：简化SM-2，对齐学校节奏

| streak | 间隔 | 对应场景 |
|--------|------|---------|
| 0 | 1天 | 今天错明天练 |
| 1 | 3天 | 本周错周末练 |
| 2 | 7天 | 单元结束练 |
| 3 | 14天 | 跨单元抽查 |
| 4 | 21天 | 期中前唤醒 |
| 5+ | 30天 | 期末唤醒 |

**辅助规则**：
- mastery_score < 0.3 → 间隔减半
- mastery_score > 0.8 → 间隔延长50%

**验收标准**：
- [ ] 知识点通过复习后 next_review_at 正确计算
- [ ] mastery_score 极低时间隔减半
- [ ] mastery_score 极高时间隔延长
- [ ] 复习会话中每个知识点生成全新变式（不重复使用旧variant记录）

---

#### F7：每日复习提醒

**触发时机**：学生启动系统时

**验收标准**：
- [ ] 有到期知识点时 → 显示："今日有 N 个知识点需要复习"
- [ ] 无到期知识点时 → 显示："今日没有需要复习的，要录入新错题吗？"
- [ ] 复习会话完成后调度状态正确更新

---

### 5.4 P2 功能详解（Phase 3）

---

#### F8：回合制战斗游戏层

**核心流程**：
```
标题画面 → 知识地图 → 选英雄 → 进入战斗
    ↑                                    ↓
    └──── Boss击败 / 每日完成 ←──────────┘
```

**战斗循环**：
```
出题 → 学生输入答案 → 判定 → 战斗动画 → 血量更新 → 下一题...
```

**动画映射**（引擎返回 action_type → 游戏渲染）：
| action_type | 游戏表现 |
|-------------|---------|
| perfect | 主角使用必杀技，大招特效 |
| correct | 主角攻击成功，敌人掉血 |
| wrong | 敌人反击，主角掉血 |
| same_error | 敌人抓住破绽暴击，主角大量掉血 |

**验收标准**：
- [ ] 战斗界面展示 hero vs enemy + HP条 + 题目
- [ ] 做对→英雄攻击动画→敌人掉血
- [ ] 做错→敌人攻击动画→英雄掉血
- [ ] 敌人HP归零→征服成功动画
- [ ] 英雄HP归零→鼓励重试

---

#### F9：IP图片支持（千人千面）

**文件夹结构**：
```
user_data/{student_name}/ip_images/
├── hero/         ← 孩子可选的主角
├── enemy/        ← 系统随机分配为敌人
└── background/   ← 可选背景
```

**验收标准**：
- [ ] 系统读取 hero/ 目录，展示为可选主角列表
- [ ] 孩子选择主角后进入游戏
- [ ] enemy/ 中的图片随机分配给知识点boss
- [ ] 文件夹为空时使用内置默认占位图（文字/emoji）
- [ ] 支持常见图片格式（png, jpg, gif）

---

#### F10：知识地图

**验收标准**：
- [ ] 展示本学期所有知识点
- [ ] 已征服的发亮，攻克的闪烁修复中，未接触的灰色
- [ ] 整体进度可视化（已征服 X / 总数 Y）

---

### 5.5 P3 功能概要（Phase 4）

#### F11：家长报告

三份报告节奏：
- **每周快报**：本周掌握 / 需要关注（3分钟看完）
- **单元报告**：该单元掌握度 + 遗留问题
- **期末报告**：全学期知识版图 + 趋势 + 假期建议

每份报告包含：
- 知识版图热力图（定性 + 定量 + 趋势）
- 需要重点关注的知识点
- 家长TODO：A类（具体活动方案）+ B类（对话引导）+ C类（节奏建议）

详见计划文档第十二章。

#### F12：课程知识库

详见计划文档第五章。三层递进：LLM预训练 → 结构化curriculum.json → 用户数据飞轮。

---

## 6. 非功能需求

| 类别 | 要求 |
|------|------|
| **响应时间** | 单次AI API调用 < 15秒（正常网络） |
| **存储** | 单个学生全学期数据 < 100MB |
| **离线** | AI功能需联网；本地数据操作完全离线 |
| **数据安全** | 错题数据仅存本地SQLite，0云端传输 |
| **零配置** | 无需注册、登录、数据库配置，安装即用 |
| **可扩展** | AI引擎层与游戏表现层接口隔离，独立可迭代 |
| **API Key** | 通过 .env 文件配置，不硬编码 |
| **OCR方案** | 使用Claude Vision API（同一SDK），一次调用完成OCR+诊断；用户确认环节保证准确性 |

---

## 7. 当前实现状态（v1.1）

**线上地址**：https://mistake-pro.zeabur.app  
**部署平台**：Zeabur（新加坡节点，Tencent Cloud 2C 4GB）  
**技术栈**：Python FastAPI + SQLite + DeepSeek API + 自渲染HTML/CSS

### 已实现功能（打勾 = 已上线）

| 功能 | 状态 | 说明 |
|------|------|------|
| F-1 账号认证 | ✅ | 注册/登录/密码，测试账号demo/demo, test/test123 |
| F0 初始设置 | ✅ | 昵称、省市区级联、年级、教材版本、科目多选 |
| F1 错题录入 | ✅ | 文字输入 + 拍照OCR（DeepSeek Vision）→ AI诊断 |
| F2 AI变式题 | ✅ | 同考点3道变式，难度阶梯easy→same→slightly_harder |
| F3 批改反馈 | ✅ | AI批改，鼓励性反馈，纠正提示 |
| F4 数据持久化 | ✅ | SQLite + /tmp 文件存储（Zeabur容器兼容） |
| 知识版图 | ✅ | 知识点掌握度列表 + 进度条 + 池状态标签 |
| 学习报告 | ✅ | 整体掌握度 + 按知识点展开 |
| 错题回顾 | ✅ | 全部错题列表，含错误类型+池状态 |
| 登录页 | ✅ | SVG渐变色logo + 手机号/密码输入 + 注册引导 |
| 底部导航 | ✅ | SVG矢量图标（首页/录入/版图/报告），选中点亮/灰暗态 |
| 全局错误处理 | ✅ | 500异常页面展示完整traceback |

### 待实现

| 功能 | 阶段 |
|------|------|
| F5 分层退出机制 | Phase 2 |
| F6 间隔重复调度 | Phase 2 |
| F7 每日复习提醒 | Phase 2 |
| F8-F10 趣味版 | Phase 3 |
| F11 家长报告（完整） | Phase 4 |
| OCR拍照→识别→确认编辑→存储闭环 | Phase 1 补充 |

---

## 8. MVP明确不做（红线）

| 不做 | 原因 |
|------|------|
| 数学以外的科目（前端已选，后端待接） | 先垂直做透数学 |
| 排名/对比/社交 | 避免制造焦虑，违背教练定位 |
| 精美3D/复杂动画 | SVG+CSS已够H5 |

---

## 8. 成功指标（上线后观测）

| 指标 | 目标 | 测量方式 |
|------|------|---------|
| 次日留存 | 孩子愿意第二天再打开 | 启动日志 |
| 单次完成率 | ≥80% 完成每次复习会话（3-5题） | 会话完成/开始比 |
| 活跃池转化率 | 2周内 active→dormant ≥60% | 数据统计 |
| 家长信息增益 | 家长看完报告能说出最薄弱点 | 定性访谈 |
| 学期覆盖率 | 期末前所有错题至少被复习过对应轮次 | 期末统计 |
